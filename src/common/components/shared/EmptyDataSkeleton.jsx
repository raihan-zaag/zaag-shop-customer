const EmptyDataSkeleton = ({ title }) => {
    return (
    <div className="w-full py-12 border border-border shadow-sm text-center bg-background">
            {title}
        </div>
    );
};

export default EmptyDataSkeleton;
